# Alembic configuration. The database URL is supplied at runtime by
# migrations/env.py (from sentinel.settings, or overridden by a caller/test),
# so no secret or connection string is committed here.
[alembic]
script_location = migrations
prepend_sys_path = .
path_separator = os
